FROM python:3.11-slim

RUN apt-get update && apt-get install -y \
    curl \
    jq \
    && rm -rf /var/lib/apt/lists/*

RUN pip install --no-cache-dir requests watchdog

WORKDIR /app

COPY scripts/state-sync.py /app/

CMD ["python3", "/app/state-sync.py"]
